{%style %}

.video-testimonials {
  background-color: var(--vt-bg-color, #ffffff);
  color: var(--vt-text-color, #1a1a1a);
  padding-top: var(--vt-padding-top, 36px);
  padding-bottom: var(--vt-padding-bottom, 36px);
  overflow: hidden;
}

.video-testimonials__container {
  max-width: 100%;
}

/* Header */
.video-testimonials__header {
  text-align: center;
  margin-bottom: 2rem;
  padding: 0 1.5rem;
}

.video-testimonials__subheading {
  font-size: 12px;
  font-weight: 600;
  letter-spacing: 2px;
  text-transform: uppercase;
  color: var(--vt-subheading-color, #666);
  margin: 0 0 0.5rem;
}

.video-testimonials__heading {
  font-size: 26px;
  font-weight: 700;
  line-height: 1.2;
  margin: 0 0 0.75rem;
  color: var(--vt-text-color, #1a1a1a);
}

.video-testimonials__description {
  font-size: 14px;
  line-height: 1.6;
  color: var(--vt-subheading-color, #666);
  margin: 0 auto;
  max-width: 500px;
  font-style: italic;
}

/* Slider wrapper — mobile full-bleed */
.video-testimonials__slider-wrapper {
  position: relative;
  margin-left: -1.5rem;
  margin-right: -1.5rem;
  padding-left: 1.5rem;
  padding-right: 1.5rem;
}

/* Swiper */
.video-testimonials__swiper {
  overflow: visible !important;
}

.video-testimonials__swiper .swiper-wrapper {
  align-items: stretch !important;
}

.video-testimonials__swiper .swiper-slide {
  height: auto;
  transition: transform 0.6s ease;
}

/* Card */
.video-testimonials__card {
  position: relative;
  border-radius: var(--vt-card-radius, 12px);
  overflow: hidden;
  cursor: pointer;
  background: #000;
  aspect-ratio: 9.6 / 12;
}

.video-testimonials__media {
  position: relative;
  width: 100%;
  height: 100%;
  overflow: hidden;
}

.video-testimonials__video,
.video-testimonials__iframe {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  border: none;
}

.video-testimonials__poster {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: opacity 0.4s ease;
  z-index: 1;
}

/* Play button */
.video-testimonials__play-btn {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 50px;
  height: 50px;
  border-radius: 12px;
  background: rgba(255, 255, 255, 0.25);
  border: 2px solid rgba(255, 255, 255, 0.6);
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  z-index: 3;
  transition: opacity 0.3s ease, transform 0.3s ease;
  backdrop-filter: blur(4px);
}

.video-testimonials__play-btn:hover {
  transform: translate(-50%, -50%) scale(1.1);
  background: rgba(255, 255, 255, 0.35);
}

/* Gradient overlay */
.video-testimonials__gradient {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 50%;
  background: linear-gradient(to top, rgba(0, 0, 0, 0.85) 0%, rgba(0, 0, 0, 0) 100%);
  z-index: 2;
  pointer-events: none;
}

/* Content overlay */
.video-testimonials__content {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 1rem;
  z-index: 3;
}

.video-testimonials__name {
  font-size: 16px;
  font-weight: 700;
  color: #fff;
  margin: 0 0 2px;
  line-height: 1.3;
  text-align: center;
}

.video-testimonials__product {
  font-size: 12px;
  color: rgba(255, 255, 255, 0.8);
  margin: 0 0 6px;
  line-height: 1.4;
  text-align: center;
}

.video-testimonials__verified {
  display: inline-block;
  vertical-align: middle;
  margin-left: 4px;
  flex-shrink: 0;
}

.video-testimonials__stars {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 2px;
}

/* Navigation buttons */
.video-testimonials__nav {
  display: none;
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  width: 42px;
  height: 42px;
  border-radius: 50%;
  background: var(--vt-btn-bg, #fff);
  color: var(--vt-btn-icon, #1a1a1a);
  border: 1px solid rgba(0, 0, 0, 0.08);
  cursor: pointer;
  align-items: center;
  justify-content: center;
  z-index: 10;
  transition: all 0.3s ease;
  box-shadow: 0 2px 12px rgba(0, 0, 0, 0.12);
}

.video-testimonials__nav:hover {
  transform: translateY(-50%) scale(0.9);
}

.video-testimonials__nav--prev {
  left: 5%;
}

.video-testimonials__nav--next {
  right: 5%;
}

/* Tablet */
@media (min-width: 768px) {
  .video-testimonials__heading {
    font-size: 36px;
  }

  .video-testimonials__content {
    padding: 1.25rem;
  }

  .video-testimonials__name {
    font-size: 18px;
  }

  .video-testimonials__product {
    font-size: 13px;
  }
}

/* Desktop */
@media (min-width: 1024px) {
  .video-testimonials__heading {
    font-size: 48px;
  }

  .video-testimonials__name {
    font-size: 20px;
  }

  .video-testimonials__slider-wrapper {
    margin-left: auto;
    margin-right: auto;
    padding-left: 0;
    padding-right: 0;
    max-width: 1200px;
  }

  .video-testimonials__nav {
    display: flex;
  }
}

{%endstyle %}

{{ 'video-testimonials.css' | asset_url | stylesheet_tag }}

<div
  class="video-testimonials section-{{ section.id }}"
  style="
    --vt-bg-color: {{ section.settings.background_color }};
    --vt-text-color: {{ section.settings.text_color }};
    --vt-subheading-color: {{ section.settings.subheading_color }};
    --vt-btn-bg: {{ section.settings.nav_button_color }};
    --vt-btn-icon: {{ section.settings.nav_icon_color }};
    --vt-card-radius: {{ section.settings.card_border_radius }}px;
    --vt-padding-top: {{ section.settings.padding_top }}px;
    --vt-padding-bottom: {{ section.settings.padding_bottom }}px;
  "
>
  <div class="video-testimonials__container">
    {%- if section.settings.subheading != blank or section.settings.heading != blank or section.settings.description != blank -%}
      <div class="video-testimonials__header">
        {%- if section.settings.subheading != blank -%}
          <p class="video-testimonials__subheading">{{ section.settings.subheading }}</p>
        {%- endif -%}
        {%- if section.settings.heading != blank -%}
          <h2 class="video-testimonials__heading">{{ section.settings.heading }}</h2>
        {%- endif -%}
        {%- if section.settings.description != blank -%}
          <p class="video-testimonials__description">{{ section.settings.description }}</p>
        {%- endif -%}
      </div>
    {%- endif -%}

    <div class="video-testimonials__slider-wrapper">
      <div class="swiper video-testimonials__swiper" id="vt-swiper-{{ section.id }}">
        <div class="swiper-wrapper">
          {%- comment -%}Render blocks twice so loop has enough slides for 2-1-2 layout{%- endcomment -%}
          {%- assign render_count = 2 -%}
          {%- if section.blocks.size >= 10 -%}
            {%- assign render_count = 1 -%}
          {%- endif -%}
          {%- for r in (1..render_count) -%}
            {%- for block in section.blocks -%}
              <div class="swiper-slide video-testimonials__slide" {% if r == 1 %}{{ block.shopify_attributes }}{% endif %}>
                <div class="video-testimonials__card">
                  <div class="video-testimonials__media">
                    {%- if block.settings.video_url != blank -%}
                      {%- assign video_url = block.settings.video_url -%}
                      {%- if video_url contains 'youtube.com' or video_url contains 'youtu.be' -%}
                        {%- if video_url contains 'youtu.be' -%}
                          {%- assign yt_id = video_url | split: 'youtu.be/' | last | split: '?' | first -%}
                        {%- else -%}
                          {%- assign yt_id = video_url | split: 'v=' | last | split: '&' | first -%}
                        {%- endif -%}
                        <iframe
                          class="video-testimonials__iframe"
                          src="https://www.youtube.com/embed/{{ yt_id }}?autoplay=0&mute=1&loop=1&playlist={{ yt_id }}&controls=0&showinfo=0&rel=0&modestbranding=1&playsinline=1"
                          allow="autoplay; encrypted-media"
                          allowfullscreen
                          loading="lazy"
                        ></iframe>
                      {%- elsif video_url contains 'vimeo.com' -%}
                        {%- assign vimeo_id = video_url | split: 'vimeo.com/' | last | split: '?' | first | split: '/' | first -%}
                        <iframe
                          class="video-testimonials__iframe"
                          src="https://player.vimeo.com/video/{{ vimeo_id }}?autoplay=0&muted=1&loop=1&background=1&playsinline=1"
                          allow="autoplay; encrypted-media"
                          allowfullscreen
                          loading="lazy"
                        ></iframe>
                      {%- else -%}
                        <video
                          class="video-testimonials__video"
                          src="{{ video_url }}"
                          muted
                          loop
                          playsinline
                          preload="none"
                        ></video>
                      {%- endif -%}
                    {%- elsif block.settings.video != blank -%}
                      <video
                        class="video-testimonials__video"
                        muted
                        loop
                        playsinline
                        preload="none"
                      >
                        {%- for source in block.settings.video.sources -%}
                          <source src="{{ source.url }}" type="{{ source.mime_type }}">
                        {%- endfor -%}
                      </video>
                    {%- endif -%}

                    {%- if block.settings.poster_image != blank -%}
                      <img
                        class="video-testimonials__poster"
                        src="{{ block.settings.poster_image | image_url: width: 600 }}"
                        alt="{{ block.settings.customer_name | escape }}"
                        width="600"
                        height="750"
                        loading="lazy"
                      >
                    {%- endif -%}

                    <button class="video-testimonials__play-btn" aria-label="Play video">
                      <svg width="20" height="20" viewBox="0 0 24 24" fill="white">
                        <path d="M8 5v14l11-7z"/>
                      </svg>
                    </button>

                    <div class="video-testimonials__gradient"></div>
                  </div>

                  <div class="video-testimonials__content">
                    {%- if block.settings.customer_name != blank -%}
                      <p class="video-testimonials__name">{{ block.settings.customer_name }}</p>
                    {%- endif -%}
                    {%- if block.settings.product_name != blank -%}
                      <p class="video-testimonials__product">{{ block.settings.product_name }}</p>
                    {%- endif -%}
                    {%- if block.settings.show_stars -%}
                      <div class="video-testimonials__stars">
                        {%- for i in (1..5) -%}
                          {%- if i <= block.settings.star_rating -%}
                            <svg width="14" height="14" viewBox="0 0 24 24" fill="#FFD700"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
                          {%- else -%}
                            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#FFD700" stroke-width="2"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
                          {%- endif -%}
                        {%- endfor -%}
                      </div>
                    {%- endif -%}
                  </div>
                </div>
              </div>
            {%- endfor -%}
          {%- endfor -%}
        </div>
      </div>

      <button class="video-testimonials__nav video-testimonials__nav--prev" aria-label="Previous slide">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <polyline points="15 18 9 12 15 6"></polyline>
        </svg>
      </button>
      <button class="video-testimonials__nav video-testimonials__nav--next" aria-label="Next slide">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <polyline points="9 18 15 12 9 6"></polyline>
        </svg>
      </button>
    </div>
  </div>
</div>

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css">
<script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js" defer></script>

<script>
  (function () {
    var sectionId = '{{ section.id }}';

    function initSection() {
      var swiperEl = document.getElementById('vt-swiper-' + sectionId);
      if (!swiperEl) return;

      var wrapper = swiperEl.closest('.video-testimonials__slider-wrapper');

      function initSwiper() {
        if (typeof Swiper === 'undefined') {
          setTimeout(initSwiper, 100);
          return;
        }

        var swiper = new Swiper('#vt-swiper-' + sectionId, {
          effect: 'coverflow',
          centeredSlides: true,
          speed: 600,
          loop: true,
          watchSlidesProgress: true,
          navigation: {
            nextEl: wrapper.querySelector('.video-testimonials__nav--next'),
            prevEl: wrapper.querySelector('.video-testimonials__nav--prev'),
          },
          breakpoints: {
            320: {
              slidesPerView: 1.4,
              coverflowEffect: { rotate: 0, stretch: 0, depth: 200, modifier: 1, slideShadows: false },
            },
            768: {
              slidesPerView: 1.6,
              coverflowEffect: { rotate: 0, stretch: 0, depth: 250, modifier: 1, slideShadows: false },
            },
            1024: {
              slidesPerView: 3,
              coverflowEffect: { rotate: 0, stretch: 0, depth: 400, modifier: 1, slideShadows: false },
            },
            1200: {
              slidesPerView: 3,
              coverflowEffect: { rotate: 0, stretch: 0, depth: 400, modifier: 1, slideShadows: false },
            },
          },
          on: {
            setTranslate: function () {
              this.slides.forEach(function (slide) {
                var p = Math.abs(slide.progress);
                slide.style.visibility = p > 2.5 ? 'hidden' : 'visible';
              });
            },
          },
        });
      }

      function pauseAllVideos(container) {
        container.querySelectorAll('video').forEach(function (v) { v.pause(); });
      }

      swiperEl.addEventListener('click', function (e) {
        var card = e.target.closest('.video-testimonials__card');
        if (!card) return;

        var video = card.querySelector('video');
        var poster = card.querySelector('.video-testimonials__poster');
        var playBtn = card.querySelector('.video-testimonials__play-btn');

        if (video) {
          if (video.paused) {
            pauseAllVideos(swiperEl);
            video.play();
            if (poster) poster.style.opacity = '0';
            if (playBtn) playBtn.style.opacity = '0';
          } else {
            video.pause();
            if (playBtn) playBtn.style.opacity = '1';
          }
        }

        var iframe = card.querySelector('iframe');
        if (iframe && iframe.src) {
          if (poster) poster.style.opacity = '0';
          if (playBtn) playBtn.style.opacity = '0';
          if (iframe.src.includes('youtube')) {
            iframe.contentWindow.postMessage('{"event":"command","func":"playVideo","args":""}', '*');
          }
        }
      });

      initSwiper();
    }

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initSection);
    } else {
      initSection();
    }

    document.addEventListener('shopify:section:load', function (e) {
      if (e.detail.sectionId === sectionId) initSection();
    });
  })();
</script>

{% schema %}
{
  "name": "Video Testimonials 1",
  "tag": "section",
  "class": "video-testimonials-section",
  "settings": [
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "OUR TESTIMONIALS"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Why people love our brand"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Don't just take our word for it, see what our customers are saying"
    },
    {
      "type": "range",
      "id": "card_border_radius",
      "label": "Card border radius",
      "min": 0,
      "max": 30,
      "step": 2,
      "unit": "px",
      "default": 12
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text color",
      "default": "#1a1a1a"
    },
    {
      "type": "color",
      "id": "subheading_color",
      "label": "Subheading color",
      "default": "#666666"
    },
    {
      "type": "color",
      "id": "nav_button_color",
      "label": "Navigation button color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "nav_icon_color",
      "label": "Navigation icon color",
      "default": "#1a1a1a"
    },
    {
      "type": "header",
      "content": "Spacing"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 36
    }
  ],
  "blocks": [
    {
      "type": "video_card",
      "name": "Video Card",
      "settings": [
        {
          "type": "video",
          "id": "video",
          "label": "Video",
          "info": "Upload a Shopify-hosted video"
        },
        {
          "type": "url",
          "id": "video_url",
          "label": "Video URL",
          "info": "YouTube or Vimeo URL (used if no Shopify video is uploaded)"
        },
        {
          "type": "image_picker",
          "id": "poster_image",
          "label": "Poster image",
          "info": "Shown before the video plays"
        },
        {
          "type": "text",
          "id": "customer_name",
          "label": "Customer name",
          "default": "Jane Doe"
        },
        {
          "type": "text",
          "id": "product_name",
          "label": "Product name",
          "default": "Product Name"
        },
        {
          "type": "checkbox",
          "id": "show_stars",
          "label": "Show star rating",
          "default": true
        },
        {
          "type": "range",
          "id": "star_rating",
          "label": "Star rating",
          "min": 1,
          "max": 5,
          "step": 1,
          "default": 5
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Video Testimonials 1",
      "blocks": [
        { "type": "video_card", "settings": { "customer_name": "Sarah M.", "product_name": "Classic Collection", "star_rating": 5 } },
        { "type": "video_card", "settings": { "customer_name": "John D.", "product_name": "Premium Series", "star_rating": 5 } },
        { "type": "video_card", "settings": { "customer_name": "Emily R.", "product_name": "Essential Kit", "star_rating": 4 } },
        { "type": "video_card", "settings": { "customer_name": "Michael T.", "product_name": "Signature Line", "star_rating": 5 } },
        { "type": "video_card", "settings": { "customer_name": "Lisa K.", "product_name": "Deluxe Bundle", "star_rating": 5 } }
      ]
    }
  ]
}
{% endschema %}
